{
  "name": "hass-omnibus-card",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --open /dev.html",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots",
    "test:update-ci": "docker run --rm -e CI=true -v $(pwd):/work -w /work mcr.microsoft.com/playwright:v1.62.1-jammy bash -c 'npm ci && npx playwright test --update-snapshots'",
    "test:ui": "playwright test --ui",
    "prepare": "cp scripts/hooks/pre-push .git/hooks/pre-push && chmod +x .git/hooks/pre-push"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "vite": "^5.4.0"
  }
}
